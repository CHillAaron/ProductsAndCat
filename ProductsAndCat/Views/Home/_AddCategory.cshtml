@model ProdAndCat
@{
}

<form action="/Add/TheCategory" method="post" class="col-5">
    <div class="form-group">
        <input hidden asp-for="ProductId" value="@ViewBag.ProductId" />
        <select asp-for="CategoryId" asp-items="@(new SelectList(ViewBag.UnassignedCats, "CategoryId", "CategoryName"))">
            <option value="" selected disabled>Pick one</option>
        </select>
         
    </div>
    <div class="form-group">
        <input type="submit" value="Add Category to this product" class="btn btn-warning" />
    </div>
</form>
